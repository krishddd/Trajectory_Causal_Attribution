gradio>=4.44
# Install agent-replay straight from the source repo (swap to
# `trajectory-causal-attribution` once it is published on PyPI).
git+https://github.com/krishddd/Trajectory_Causal_Attribution.git@main
